/*
var data = [5, 64, 6, 54, 64, 65, 6, 5, 65, 65, 46, 5].sort((a, b) => 
{
	return a - b // sorting in ascending order
})

console.log('Data provided: ', data) // checking the data

// Calculate max sum
function maxSum(arr)
{
	let max_sum = 0
	arr.forEach((value) =>
	{
		max_sum += value
	})
	return max_sum
}
const max_sum = maxSum(data)

const mean = max_sum / data.length // Mean of Data

// var a=[[1,'s'],[3,'f'],[2,'j']]
var a = [40, 36, 111, 111111111111, 104, 360, 065, 60, ]

console.log(a.sort())

console.log('Sum: ', max_sum)
console.log('Mean: ', mean)
*/

a = []
b = {}

console.log(typeof b)

function isObject(b)
{
	if (a == Array.prototype)
	{
		return 0
	}
	else
	{
		return 1
	}
}

console.log(Array.prototype)